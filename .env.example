# Exchange API keys (optional, needed for live data)
BINANCE_API_KEY=
BINANCE_SECRET=
# Default exchange
DEFAULT_EXCHANGE=binance
# Data cache directory
DATA_CACHE_DIR=data_cache
# Runs directory
RUNS_DIR=runs
# Proxy (if needed to access exchange APIs)
HTTPS_PROXY=http://127.0.0.1:7897
